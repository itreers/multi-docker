name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t itreers/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true itreers/react-test npm test

      - run: docker build -t itreers/multi-client ./client
      - run: docker build -t itreers/multi-nginx ./nginx
      - run: docker build -t itreers/multi-server ./server
      - run: docker build -t itreers/multi-worker ./worker

      - run: docker push itreers/multi-client
      - run: docker push itreers/multi-nginx
      - run: docker push itreers/multi-server
      - run: docker push itreers/multi-worker
